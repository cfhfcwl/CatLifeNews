# ThinkPHP 8 Nginx 伪静态规则（宝塔面板 → 网站 → 伪静态 中粘贴此内容）

location ~* (runtime|application)/ {
    return 403;
}

location / {
    if (!-e $request_filename) {
        rewrite ^(.*)$ /index.php?s=/$1 last;
        break;
    }
}
